<Window x:Class="dubao.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:dubao"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left">
            <Label Content="Nhập số"></Label>
            <TextBox x:Name="txtnhapso" Text="{Binding nhapso, Mode=TwoWay}" Width="120"></TextBox>
        </DockPanel>
        <Button Content="Chay" Grid.Row="0" Grid.Column="1" Width="70" HorizontalAlignment="Left" Click="Button_Click" CommandParameter="{Binding Text, ElementName=txtnhapso}"></Button>
        <TextBox Text="{Binding result, Mode=TwoWay}" Grid.Row="2" Grid.Column="2" MaxLines="100" MinLines="5"></TextBox>
    </Grid>
</Window>
